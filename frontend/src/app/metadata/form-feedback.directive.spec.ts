import { FormFeedbackDirective } from './form-feedback.directive';

describe('FormFeedbackDirective', () => {
  it('should create an instance', () => {
    const directive = new FormFeedbackDirective();
    expect(directive).toBeTruthy();
  });
});
